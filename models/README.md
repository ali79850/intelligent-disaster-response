Model checkpoints are not committed to this repository (see root `.gitignore`).
Trained model artifacts will be made available via external storage (TBD in
Phase 5) once training begins. This file exists so the `models/` directory
structure is preserved in git despite being otherwise empty.